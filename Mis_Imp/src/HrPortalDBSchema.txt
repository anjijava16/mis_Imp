FOREIGN KEY (`USER_ID`) REFERENCES `users` (`USER_ID`),



CREATE TABLE EMPLOYEE_PERSONAL_TBL(
EMP_ID VARCHAR(10),
EMP_NAME VARCHAR(100),
EMP_PROJECT_NAME VARCHAR(90),
EMP_TECH_MODULE VARCHAR(256),
EMP_DOB DATE,
EMP_JOIN_DATE DATE,
EMP_REL_DATE DATE,
EMP_ADDRESS VARCHAR(256),
EMP_BOND_START_DATE DATE,
EMP_BOND_END_DATE DATE,
EMP_SKYPE_ID VARCHAR(60),
EMP_DOCUMENTS VARCHAR(128),
EMP_COMMENTS VARCHAR(256),
EMP_CARD_TYPE VARCHAR(60),
PRIMARY KEY(EMP_NAME),
FOREIGN KEY (`EMP_NAME`) REFERENCES `employee_primary_tbl` (`EMP_NAME`),
)

CREATE TABLE `employee_primary_tbl` (
	`EMP_ID` VARCHAR(10) NULL DEFAULT NULL,
	`EMP_NAME` VARCHAR(100) NOT NULL DEFAULT '',
	`EMP_EMP_ORG` VARCHAR(100) NULL DEFAULT NULL,
	`EMP_IMG_LOCATION` VARCHAR(100) NULL DEFAULT NULL,
	`Emp_IMG_TYPE` VARCHAR(60) NULL DEFAULT NULL,
	`EMP_IMG_SIZE` INT(10) NULL DEFAULT NULL,
	`EMP_ROLE` VARCHAR(100) NULL DEFAULT NULL,
	`EMP_REG_TIMESTAMP` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
	`EMP_REG_DATE` DATE NULL DEFAULT NULL,
	`EMP_COMMENTS` VARCHAR(256) NULL DEFAULT NULL,
	`EMP_PHONE` MEDIUMTEXT NULL,
	`EMP_EMAIL` VARCHAR(60) NULL DEFAULT NULL,
	PRIMARY KEY (`EMP_NAME`)
)

CREATE TABLE `employee_report_tbl` (
	`EMP_ID` VARCHAR(10) NULL DEFAULT NULL,
	`EMP_NAME` VARCHAR(100) NOT NULL DEFAULT '',
	`EMP_REPORT_YEAR` INT(10) NULL DEFAULT NULL,
	`REPORT_NAME` VARCHAR(10) NULL DEFAULT NULL,
	`REPORT_INFO` VARCHAR(2000) NULL DEFAULT NULL,
	`REPORT_COMMENTS` VARCHAR(10) NULL DEFAULT NULL,
	`REPORT_WORK_DETAILS` VARCHAR(2018) NULL DEFAULT NULL,
	PRIMARY KEY (`EMP_NAME`),
FOREIGN KEY (`EMP_NAME`) REFERENCES `employee_primary_tbl` (`EMP_NAME`)
)

